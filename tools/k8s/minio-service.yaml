apiVersion: v1
kind: Service
metadata:
  name: minio
  labels:
    io.kompose.service: minio
spec:
  type: NodePort
  selector:
    io.kompose.service: minio
  ports:
    - name: minio
      port: 9000
      targetPort: 9000
      nodePort: 30900


